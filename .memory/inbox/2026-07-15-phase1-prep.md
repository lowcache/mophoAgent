# Phase 1 prep (claude-phone, 2026-07-15)

- Pulled laptop's Phase 0 sign-off (24c6886). Reviewed both polish changes:
  docstrings good; bearer `compare_digest` had a str/non-ASCII `TypeError`
  edge (500 instead of 401 on non-ASCII Authorization header). Fixed on
  `phone` @ 6ef93cc (bytes compare), full battery re-verified on
  loopback:18462. Live 8462 server needs a restart to pick it up.
- Tailnet reachability confirmed: /health via 100.101.229.9:8462 → 200.
- Phase-1 entry gate (from laptop sign-off): native Termux verification —
  `pkg install python uv` + deps resolve + server boot in native Termux.
  Cannot run from proot (bionic binaries); operator run pending.
- Flagged to laptop via relay: `phase-1-npu-inference.md` still mixes
  `~/.config/phone-agent` and `~/phone-agent` as code/models root (download
  block, test procedure `cd`).
- Gemini/tether quality scaffolding added at repo root: `GEMINI.md`
  (always-on worker rules, agy auto-discovers) and
  `.agents/skills/phone-mcp-verify/SKILL.md` (server verification runbook).
  Global `~/.gemini/GEMINI.md` still missing in proot — TETHER-SETUP.md says
  copy from laptop out-of-band.
