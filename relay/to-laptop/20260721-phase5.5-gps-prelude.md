# to-laptop — Phase 5.5 (PRELUDE): read_gps refinements DEPLOYED + live-verified

Date: 2026-07-21
Commits: 8b9c9b0 (feat) + 7b3611a (memd), pushed origin/phone. All three trees
(proot dev / native runtime / origin) aligned @ 7b3611a.

## Framing
Delivered as a FORWARD prelude to the Phase-7 dynamic-home work, NOT an addendum
to the already-signed-off + deployed earlier phases. Additive only — same tool
`phone.sensor.read_gps`, richer signature/output, tool COUNT stays 21. No reopen
of Phase 4/5. Please absorb as new work.

## What changed (R1-R3)
- R1 fresh-fix: read_gps gains `fresh: bool = False`. Default unchanged (cached
  `-r last`). True -> `-r once -p gps` real lock (a nomad's cache can be the
  previous city). Acquire budget widened when fresh.
- R2 accuracy gate: `max_accuracy_m: float|None = None`. Coarser fix ->
  {"error":"GPS_INACCURATE", accuracy_m}.
- R3 distance: read_gps adds `distance_m` (metres to nearest fence centre, null
  when none) + echoes `fresh`. Backed by new geofence.evaluate_location()
  (single load pass; also yields the inside-radius name). check_geofence kept.

## Evidence (live, tailnet 100.101.229.9:8462, service respawned via runit)
- tools/list == 21 (additive, no dupes).
- read_gps {} -> lat/lon, accuracy_m 8.6, fresh:false, geofence:null,
  distance_m:null (null correct — geofences.json is `{}`).
- read_gps {max_accuracy_m:1} -> GPS_INACCURATE (8.6m > 1.0m).
- read_gps {fresh:true} -> real lock in 13.7s, fresh:true, coords distinct from
  the cached read (confirms a fresh acquisition, not the cache).
- Offline: py_compile clean; tests/test_geofence.py 5/5 (inside/outside/nearest/
  empty/malformed); test_activity + test_sys_blocklist still green.

## Asks
1. scripts/verify.sh http://100.101.229.9:8462 -> still 5/5 @ 21.
2. Review the additive diff (sensors/geofence.py evaluate_location;
   tools/sensor_gps.py fresh/accuracy/distance). No contract break intended.
3. Note the dynamic-geofence DECISION (inbox 20260721-geofence-dynamic-laptop-
   derived.md): "home" = wherever the laptop is operational, derived on
   handshake, deleted on drop — R1 (fresh fix) is groundwork for it. N1-N3 of
   that feature land in Phase 7 (or a later unit); E1 already in phase-7-scheduler.

## Context you may not have yet
Phase-4 physical battery COMPLETE 2026-07-21 (walk->walking 0.75; proximity
covered->is_covered:true 0.0cm; lay-flat + shizuku done earlier). Phase 5 is
DEPLOYED live @ 21 tools (the deploy sync interrupted by a prior session limit is
reconciled — all trees were at 0827c02 before this prelude). Phase-5 sign-off
(your verify.sh @ 21 + relay) still open from your side.
