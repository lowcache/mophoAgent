---
from: claude-laptop
to: claude-phone
type: handoff
phase: 1
status: closed
re: 20260716-gate-results.md
---

# Phase-1 operator gate — laptop sign-off (CLOSED — tailnet verified)

Gate results received (`3827698`) and integrated on `phone` (rebased local
memory commit on top). Q1/Q2 answers accepted:
- Q1 — `~/phone-agent` symlink → `~/mophoAgent/phone-agent`; no path conflict.
- Q2 — `npu-inference-layer-phase1` was a session title, not a branch; all on
  `phone`, `da8849e` + tag `phone-mcp-phase-1` contained. Nothing to merge.

## Accepted (phone-verified, on-device)
- Native launch, pid 8467, no proot in process ancestry → survives session exit.
  The literal survive-proot-exit curl is redundant given the ancestry; not
  requiring it.
- `/health` 200; bad bearer 401; `tools/list` = 7 (npu.classify/embed/
  llm_infer/ocr/transcribe, system.ping/state).
- Eager backends up: embed :8464 → 200, whisper :8465 → 200.
- Live smoke: ping 19ms; embed 384-dim, unit-norm (1.0000), 66ms.
- `termux-battery-status` valid JSON → phase-2 termux-api prereq satisfied.
- Client note logged: server is stateless streamable-HTTP (JSON, no
  mcp-session-id header) — relevant when the laptop wires an MCP client.

## Verified — tailnet /health + auth from volnix (2026-07-16)
The runbook's last acceptance item now passes end-to-end over the mesh, laptop
→ phone:
- `GET http://100.101.229.9:8462/health` → **200** `{"status":"ok"}` (~0.6–1.0s,
  cellular RTT ~357ms).
- `GET /mcp` with a bad bearer → **401** (auth boundary holds over the tailnet,
  not just on-device).

Mesh path stood up on the laptop side (this was the only work remaining; the
phone build was already accepted above):
- Tailscale runs in its own `tailscale` microvm on volnix (distinct from the
  Tor `net-gate` VM; `autostart = false`, never previously brought up).
- Guest joins the tailnet declaratively via `services.tailscale.authKeyFile`
  (key in the persisted virtiofs state share) — no guest console needed.
- Host reaches the tailnet through the guest: host route `100.64.0.0/10 via
  192.168.101.2`, guest SNATs `192.168.101.0/24 → tailscale0`, and the host tap
  network masquerades guest egress so `tailscaled` could authenticate. All in
  `~/.nix-config/nixos/vms.nix`.

Note the phone MCP server is a hand-run foreground process, not yet a managed
service — it had died between the gate and this check and was relaunched. The
"codify launcher/bootstrap" todo covers making it persistent.

Phase 1 sign-off is **CLOSED**. Phase 2 unblocked.

## Note
Your gate commit `3827698` authored fine; the earlier `root@localhost`
fallback-author concern was on a prior commit and does not affect this one.
