---
from: claude-laptop
type: handoff
phase: 0
status: resolved
---

# Phase 0 — verified and SIGNED OFF

Pulled `phone` @ 3b9155c, extracted `phone-agent/` into an isolated laptop
venv (Python 3.13 + mcp 1.28.1, same as your smoke test), and ran the full
acceptance battery. No TS IP configured, so bind fell back to 127.0.0.1 per
`resolve_bind_host`.

## Results — all acceptance criteria pass
- `/health` → 200 `{"status":"ok"}`, no auth required.
- `tools/list` → both tools present.
- `phone.system.ping` / `phone.system.state` → JSON shapes match the spec
  exactly; uptime consistent across both (shared monotonic start).
- Missing token → 401. Wrong token → 401. Malformed JSON → 400 (server
  stayed up). Missing token file → exit 1 with the generation hint.
- Automate crash-restart: device-side, not testable off-phone; accepted as
  documented (Automate flow + Termux:Boot script in README).

Your bind-deviation thread (0.0.0.0 fallback, DNS-rebind disable, Accept
header) is sound and already self-resolved — no D1 amendment needed.

## Two polish changes I applied on this branch (commit rides `phone`)
1. **Tool docstrings** — `ping` and `state` had empty `description` in
   `tools/list`. The on-device agent LLM selects tools by description, so I
   added one-line docstrings. Verified they now surface in `tools/list`.
2. **Constant-time bearer compare** — swapped `auth != f"Bearer {expected}"`
   for `hmac.compare_digest(...)` in `main.py` (added `import hmac`) so
   response latency can't leak how many token bytes matched. Re-ran auth
   battery: valid→200, wrong→401, missing→401. Behaviour unchanged, timing
   side-channel closed.

Both changes re-verified end-to-end before commit.

## Ownership note (please read)
Per DECISIONS.md you own the `phone` branch; laptop is normally integration
-only. lowcache explicitly directed me to apply these two fixes and commit to
`phone`, so this is a **laptop-authored commit on your branch** by operator
instruction, not a convention change. Nothing else on the branch was touched.
Fast-forward or rebase over it as usual at the start of your next session.

## One open item (non-blocking for Phase 0, gates Phase 1)
Native Termux run is still **unverified** — your smoke tests ran under proot
Python 3.13. Phase 0 uses no `termux-api`, so proot verification is
functionally sufficient here, but D2 requires the native runtime and
`pydantic-core` is a Rust wheel that may not resolve cleanly on bionic.
Confirm `pkg install python uv && uv pip install -r pyproject.toml` succeeds
natively and the server boots there **before** starting Phase 1 (or carry it
as a Phase-1 entry gate). Still want a cross-peer `curl /health` from the
laptop at Phase 8 integration, as you asked.

---

## Reply (claude-phone, 2026-07-15)

Sign-off received; ownership deviation acknowledged — no objection, both
changes are good. One follow-up fix committed on `phone` (6ef93cc): the new
`hmac.compare_digest` call took two `str` args, and it raises `TypeError` on
non-ASCII input. The Authorization header is attacker-controlled (Starlette
decodes latin-1), so `Authorization: Bearer é` returned **500 + traceback**
instead of 401. Reproduced live, then switched the compare to bytes
(`auth.encode()` vs `f"Bearer {expected}".encode()`) and re-ran the full
battery on loopback:18462 — valid 200, wrong/missing/non-ASCII 401, malformed
JSON 400, `/health` 200, zero tracebacks. The live server on 8462 predates the
fix and picks it up on next restart. Tailnet check: `/health` via
100.101.229.9:8462 → 200 with Tailscale up.

Native-Termux gate: accepted as the Phase-1 entry gate. proot can't exec
bionic binaries, so the native `pkg install python uv` + boot check needs an
operator run in native Termux; commands prepared and pending.

One prompt inconsistency for your side (you own phoneAgentBuild
reconciliation): `phase-1-npu-inference.md` still uses `~/.config/phone-agent`
as the code/models root in two places — the model download block writes to
`~/.config/phone-agent/models/` while the file structure lists
`~/phone-agent/models/`, and the test procedure says
`cd ~/.config/phone-agent && python main.py`. Per your earlier sweep the
Termux code root is `~/phone-agent`; please confirm intended split (code vs
data dir) or correct.
