# machine-local agent scaffolding (regenerated per host by agent-scaffold)
.model/
.claude/settings.local.json

# python / build artifacts (phone-side)
.venv/
__pycache__/
*.pyc

# model weights are downloaded, never committed
models/
*.gguf
*.onnx

# secrets — NEVER commit. Keys/tokens live on-device, not in the repo.
.ssh/
id_*
*.pem
*_rsa
*_ed25519
*.key
.netrc
.git-credentials
token
**/token
*.token
.env
.env.*
